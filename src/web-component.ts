import IsaWizardComponent from './IsaWizardElement.ce.svelte';

/**
 * ISA Wizard Web Component
 * Compiled by Svelte as a native custom element (see IsaWizardElement.ce.svelte).
 * Importing this module registers <isa-wizard>.
 *
 * Usage: <isa-wizard config-url="config.json"></isa-wizard>
 * Or set properties directly: widget.config = {...}; widget.onFinish = (data) => {...}
 */
const IsaWizardElement = IsaWizardComponent.element!;

export { IsaWizardElement };
export default IsaWizardElement;
